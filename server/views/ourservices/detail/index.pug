extends ../../layout/index
block content
  section#mainContent2
    header#contentHeader.hasCrumbs
      h3.card-title.ourserviceslist-title=service.title
    article#mainArticle
      .multiCol
     
      p.ourserviceslist-info
        a(href=`/ourservices/${service.shortname}`)  
        img.ourserviceslist-img.card-img-top.pull-left(src=`/images/${service.shortname}.jpg`, alt=`Photo of ${service.shortname} Post`)
      h5.card-title.ourserviceslist-title=service.title
      !=service.description
  include ../../includes/sidebar
